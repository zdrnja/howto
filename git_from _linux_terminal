#let's assume that you already added ssh keys to github and made a repo that you want to push to.
mkdir howto
cd howto 
emacs README.md # say something smart
emacs .gitignore # *.temp.binary
emacs code.c
git init
git add *
git commit -u "smart comment"
git remote add origin git@github.com/zdrnja/[some existing repo].git
git push -u origin master
